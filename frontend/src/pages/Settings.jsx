import {PageHeader,Panel} from '../components/UI';
export default function Settings(){return <div className="page"><PageHeader eyebrow="CONFIGURATION" title="Settings" subtitle="Runtime values are backend-managed through application.properties and environment variables."/><div className="grid-2"><Panel title="AI configuration"><div className="kv"><span>Ollama URL</span><code>OLLAMA_URL</code><span>Model</span><code>OLLAMA_MODEL</code><span>Default</span><strong>qwen2.5:3b</strong></div></Panel><Panel title="Kafka configuration"><div className="kv"><span>Bootstrap</span><code>KAFKA_BOOTSTRAP_SERVERS</code><span>AI topic</span><code>raw-ai-logs</code><span>Consumer group</span><code>logfusion-ai-workers</code></div></Panel></div><Panel title="Deployment note" subtitle="Read-only until authenticated admin APIs exist."><p className="muted">For production, expose validated admin endpoints rather than allowing the browser to directly mutate Kafka or Ollama configuration.</p></Panel></div>}
