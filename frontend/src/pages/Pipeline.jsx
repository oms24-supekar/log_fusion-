import {useMemo} from 'react';import {useLogs} from '../lib/hooks';import {PageHeader,Panel,Stat} from '../components/UI';
export default function Pipeline(){const{logs}=useLogs({interval:4000});const m=useMemo(()=>({queued:logs.filter(x=>x.processingStatus==='AI_QUEUED').length,processing:logs.filter(x=>x.processingStatus==='AI_NORMALIZING').length,failed:logs.filter(x=>['AI_QUEUE_FAILED','AI_FAILED'].includes(x.processingStatus)).length}),[logs]);return <div className="page"><PageHeader eyebrow="EVENT STREAMING" title="Kafka pipeline" subtitle="Application-observed workflow; live broker lag needs a dedicated backend metrics endpoint."/><div className="stats"><Stat label="Queued" value={m.queued} meta="AI_QUEUED"/><Stat label="Normalizing" value={m.processing} meta="AI_NORMALIZING"/><Stat label="Failed" value={m.failed} meta="queue / worker failures"/></div><Panel title="AI event path"><div className="pipeline large">{['Spring ingestion','raw-ai-logs','logfusion-ai-workers','Ollama','Validation','PostgreSQL'].map((x,i)=><div key={x} className={i===1||i===2?'ai-step':''}><span>{i+1}</span><strong>{x}</strong><small>{['Producer','Kafka topic','Consumer group','qwen2.5:3b','Field review','normalized_logs'][i]}</small></div>)}</div></Panel><Panel title="Configured topics"><div className="topic-grid"><div><span>AI jobs</span><code>raw-ai-logs</code></div><div><span>Normalized stream</span><code>normalized-logs</code></div><div><span>Dead letter queue</span><code>ai-processing-dlq</code></div></div></Panel></div>}
