import {useNavigate} from 'react-router-dom';import {Badge} from './UI';import {formatDate} from '../lib/utils';
export default function LogTable({logs=[]}){const nav=useNavigate();return <div className="table-wrap"><table><thead><tr><th>Received</th><th>Source</th><th>Type</th><th>Format</th><th>Status</th><th/></tr></thead><tbody>{logs.map(l=><tr key={l.id} onClick={()=>nav(`/logs/${l.id}`)}><td>{formatDate(l.receivedAt)}</td><td><strong>{l.sourceName||'—'}</strong><small className="mono-block">{l.id}</small></td><td>{l.sourceType||'—'}</td><td><Badge value={l.detectedFormat}/></td><td><Badge value={l.processingStatus}/></td><td className="arrow">→</td></tr>)}</tbody></table></div>}
